# 开奖闭环核对报告 —— 26101 期（2026-09-01 周二）

- 核对时间：2026-09-02 18:00-18:30
- 触发方式：用户问询"该做的都做了？"→ 逐项实测（非凭印象）

## 1. 开奖入库 ✅

主表 `ssq_master.csv` 末行 `26101,05,06,08,09,24,25,12`，行数 3498。
期号链条核对：26100=2026-08-30（周日）→ 26101=2026-09-01（周二）✓。

## 2. 官方双源核对 ✅（数据无污染）

| 来源 | 26101 号码 | 一致性 |
|---|---|---|
| 中国福彩官网 API（cwl.gov.cn findDrawNotice） | 红 05,06,08,09,24,25 蓝 12 | ✓ |
| 深圳福彩官网公告（szlottery.org）/ 腾讯新闻 | 红 05,06,08,09,24,25 蓝 12 | ✓ |

主表与官方完全一致。本期一等奖 4 注（吉林/甘肃/深圳2），奖池 7.66 亿。

## 3. 预注册预测打分 ✅（但发现调度缺陷，见 §5）

- 登记：9/1 18:00:06（开奖前，时间戳已固化）：引擎 [1,7,10,17,26,28]+11 vs 随机基线 [4,5,12,24,26,29]+2
- 打分：9/2 17:52（开机补跑）DONE exit=0；predictions.jsonl 待打分 = 0
- 本期：引擎红 0/6 蓝未中；基线红 2/6 蓝未中；null 检验 p_combined=0.9980
- 累计 n=9：引擎红球总命中 8（均值 0.89） vs 基线 14（1.56）；蓝 0/0
  - **功效说明（n 很小）**：期望红球命中 1.09/期。n=9 时引擎 8 vs 期望 9.8，
    波动范围内，**与随机不可区分**（基线 14 也只是运气好的波动）。
    n=9 的任何结论都无统计功效——这是预注册检验的预期路径，不是失败。

## 4. OBF 序贯确认链（PRE_REGISTERED_PROTOCOL_v1）✅

- 锚点完整性：`(True, 一致(registry+protocol+boundary))` —— 预注册未被篡改
- 当前进度：基准 n=3496，新开奖 2 期（26100, 26101）/ 终点 3500
- **n_new=2 < 50 ⇒ INSUFFICIENT，按设计不打分**（单期对 33 维向量是纯噪声）。
  首次有效 OBF 打分约在 n≥50（约 17 周后）。
- 修复：`--status` 对 v2 账本记录（键名 `status` 而非 `verdict`）崩溃
  （KeyError），已做向后兼容并实测通过。本次打分已记账
  （`audit/preregistered_scores.jsonl`，INSUFFICIENT @ n=2）。

## 5. ⚠️ P0 发现：score 计划任务 22:30 触发从未生效

**实测证据（非推测）**：
- `predict_cron.log`（8/18 起）中 phase=score 共 8 次，时间全在 17:49-18:02，
  **没有一次是 22:30**；而 register 任务 6 个计划窗口 5 次准点 18:00:0x。
- 任务计划引擎动态状态（Get-ScheduledTaskInfo）：score `LastRunTime=9/2 17:52:46`
  = 今天开机补跑（PC 在 9/1 22:30 关机/睡眠，StartWhenAvailable 在唤醒时补跑）；
  `NextRunTime=9/3 22:30`（触发器已注册）。**任务计划操作日志通道未启用，无法取证更早窗口**。
- 后果：开奖夜打分实际依赖"第二天开机"兜底，闭环延迟 ~20 小时；若连续关机
  多日，确认链和入库都会滞后。

**修复（已写好，需一次管理员执行）**：`D:\ssq_evo\fix_score_task.ps1`
重建 score 任务为三重冗余触发（22:30 主 + 23:30 夜兜 + 次日 09:00 晨兜），
脚本幂等、打分幂等（已打分自动跳过、合并 0 期无害）。执行：

```
powershell -ExecutionPolicy Bypass -File D:\ssq_evo\fix_score_task.ps1
```

另：主表 mtime=9/2 18:13:43，晚于 17:52 打分且来源未明（内容已验证与官方一致，
不构成数据污染）；若非用户本人操作请告知。

## 6. 引擎状态 ✅（退役为预期状态）

- `ENGINE_RETIRED` flag 存在；Docker 停机 = 预期，非故障
- watchdog v4 closeout 模式心跳正常（15 分钟节拍，OK）
- 退役后存活仪器本次全部实测：predict_cron 登记/打分 ✓、preregistered_scorer ✓、
  锚点校验 ✓

## 7. 检查清单总览

| # | 项目 | 结果 |
|---|---|---|
| 1 | 开奖入库 | ✅ 26101 已入库，行数 3498 |
| 2 | 官方双源核对 | ✅ 三个独立来源一致 |
| 3 | 预注册预测打分 | ✅ 待打分=0（n=9 与随机不可区分） |
| 4 | OBF 预注册探针 | ✅ 锚完整；n=2 INSUFFICIENT（设计如此）；status bug 已修 |
| 5 | 引擎状态 | ✅ 退役 flag 在，watchdog 心跳正常 |
| 6 | 累计战绩 | ✅ n=9：引擎红 0.89/期 vs 期望 1.09，无功效不下结论 |
| 7 | **调度健壮性** | ⚠️ **22:30 触发从未生效 → fix_score_task.ps1 待用户执行** |

---

即便确认 σ≈3.5% 边际偏倚，它不改变头奖概率的量级（1/1772 万）。这是结构，不是印钞机。
