# 引擎复活决策记录（ENGINE REVIVAL）

- **日期**：2026-09-24 17:35 (UTC+8)
- **决策人**：用户（"开工"指令，此前连续追问"计算方式有没有检验与纠正""引擎有没有用"）
- **性质**：研究决策。复活对象从"无监督 GA 搜索"改为**受约束的工具生产线**，附新的产出验收纪律。

## 1. 复活依据（相对 8/31 退役时的认知增量）

1. **阳性对照揭示了方法学空洞**（`audit/PREDICTION_POSITIVE_CONTROL_20260924.md`）：
   生产主力 `signal_regime_red_gap_max` 对真实偏倚（注入 σ=4.63%）双向皆盲；
   而引擎进化产物 `comp_ensemble` 是唯一通过双向对照的选号方法
   （有偏 z=1.98 检出 / 均匀无假警报）。
2. **引擎的产物并非全废**——退役把唯一有分辨力的工具一并带走了（8 棵树冻结、只剩 1 棵）。
3. 8/31 退役理由第 2 条（"假设类搜索已饱和"）针对的是**重排类零假设**上的搜索；
   它未否证"进化复合公式树能提取静态偏倚信息"——今天的数据恰恰是正面的。

## 2. 复活执行记录（按 ENGINE_RETIREMENT_20260831.md §5）

| 步 | 动作 | 结果 |
|---|---|---|
| 1 | compose 文件恢复（取 .RETIRED 9/6 权威版） | ✅ |
| 2 | `GIT_SHA=1c116bf docker compose up -d --build` | ✅ 容器 Up，99.5% CPU bootstrap（§5 预告，10-15 分钟） |
| 3 | 删除 `ENGINE_RETIRED` flag（备份于 `audit/ENGINE_RETIRED_flag_20260924.bak`） | ✅ watchdog 下一轮（15 分钟内）自动回全量监督 |
| 4 | `verify_deployment.py` | 6/7 PASS；唯一 FAIL=state.json 陈旧（bootstrap 完成即消解） |
| 5 | watchdog 全量体检 | 待下一轮（flag 已删，自动切换） |

## 3. 复活附加纪律（本次新增，与上次"意外复活"的本质区别）

**红线不变**：禁止无监督自演进以过闸为目标搜索（诚实护栏契约第 1 条）。
本次复活在此之上加三条产出验收纪律：

1. **新树双向对照**：frontier 每新增一棵 comp 树，必须跑有偏（预注册向量注入）+
   均匀（零假设）双向合成对照；有偏检出且均匀无假警报才准进 `comp_elites`。
   复测脚本：`audit/_positive_control_comp.py`。
2. **前瞻预注册**：新树进入生产选号前，须以冻结姿态前瞻 ≥30 期，走 OBF 同款纪律。
3. **监督在岗**：watchdog 全量模式恢复是复活的前置条件而非事后检查（§5 第 5 步）。
   若 watchdog 连续 2 小时无心跳，predict_cron 报警、引擎应停。

## 4. 今晚（26111 开奖日）时序说明

- 18:00 register：与本复活无关，照常（用的是 frontier 现存的 1 棵冻结树，非今晚新生成）
- 21:15 开奖 / 22:30 score：照常
- 引擎 bootstrap 完成后进入 data_driven 模式，等 26111 入库才动，不会干扰收盘管线

## 5. 回退程序（随时可用）

`docker stop ssq-evo-engine` + 恢复 flag：
`cp audit/ENGINE_RETIRED_flag_20260924.bak D:/ssq_evo_data/ENGINE_RETIRED`。
两动作可逆、互不依赖。

---
> 页脚：即便确认 σ≈3.5% 边际偏倚，它不改变头奖概率的量级（1/1772 万）。这是结构，不是印钞机。
