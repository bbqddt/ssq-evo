# PRE_REGISTERED_PROTOCOL_v1 —— 前瞻序贯确证协议

**生效**: 2026-09-01 提交即生效（git commit 见锚点）。首个适用开奖: 2026-09-01（第 26101 期）。
**基线**: 沿用 2026-08-29 20:39:20 锚定的注册向量（`audit/marginal_bias_preregistered.json`，
n_basis=3496，sha256 见 `anchors/preregistered.sha256` 第一行）。**不重置基线、不换方向、不换数据。**

## 1. 唯一确证统计量（df=1）

    w   = normalize(center(注册向量 dev_pct[1..33]))
    l_d = Σ_{b∈第d期开奖} w_b            （i.i.d. 增量，每期恰 6 个不同球）
    Z_n = Σ_{d≤n} l_d / (SD1·√n),  SD1² = 6·27/(33·32) = 162/1056

H0（p 均匀 i.i.d.）下 l_d 独立同分布 ⇒ Z_n 精确渐近 N(0,1)，且
corr(Z_m, Z_n) = √(m/n) —— 恰为布朗运动信息结构，故 Lan-DeMets OBF 花费**精确成立**。

选此统计量的物理依据：候选机制 = 球体物理差异 → **一阶边际**非均匀（方向已知=注册向量）。
已知方向的线性得分检验是该备择下的 Neyman-Pearson 最有力检验。
**永久放弃**在同一前瞻窗口检验 pairwise/run/segment/周期等（那是探索，不属确证）。

## 2. 序贯设计

- **单侧 α = 0.05**，花费函数 α\*(t) = 1 − Φ(z_α/√t)（OBF），t = n/3500
- **看点**: 每期开奖后（cron 自动，约 150 次/年）；n<50 无正式看点（边界为天文级）
- **边界表**: `audit/obf_boundary.csv`（McPherson 递推生成，sha256 锚定；打分器只查表不现算）
- **停止规则**:
  - Z_n ≥ b(n) → **CONFIRMED_OBF**（注册方向静态偏倚获前瞻确认）⇒ 停止
  - n = 3500 未穿越 → **NOT_CONFIRMED_AT_DESIGN** ⇒ 停止。
    含义：σ≈3.5% 方向候选在功效 99.8% 的设计下未获确认 ⇒ **该候选降级**。
    （措辞边界：这是对**该候选**的证伪级证据，不是"域为 null"——见 ARCHITECTURE.md §7）

关键边界：b(200)=6.98, b(500)=4.51, b(1000)=3.30, b(1500)=2.79, b(2000)=2.49, b(3500)=2.05。
（终点边界 2.05 > 一次性 1.645：这是 3450 次合法窥视的代价，已计入功效表。）

## 3. 设计标定（双向，obf_design.py 生成，audit/obf_design.json）

| 项目 | 数值 | 判定 |
|---|---|---|
| 阴性 FPR（均匀随机全离散路径，n=400） | **5.00%** | ✅ 回名义（二项 95%CI [3.1%, 7.5%]） |
| 布朗递推自检 FPR（n=4000） | 4.50% | ✅ 与 5% 相容 |
| 阳性功效 σ=3.5%（离散全路径，n=400） | **99.75%**，中位首跨 **n=1189（≈7.9 年）** | ✅ |
| 布朗近似功效（n=20000） | 41.7%@1000 · 83.9%@1500 · 96.7%@2000 · ~100%@3500 | ✅ 与离散一致 |

设计过程中抓到并修复的 bug（留档防复发）：
① 诊断用 bincount 索引错位（0 基 vs 开奖号 1 基）制造过假 sd；② 布朗漂移漏乘"每期 6 球"
包含概率因子 6，曾把功效低估 5 倍。**改闸门必须双对照**，本次再次验证。

## 4. 双轨记账规则（不可协商）

1. **历史段**（≤ n_basis=3496）：一切报告保持 **df=13**（analysis_ledger 管辖），结论不变：
   "当前数据+当前闸门+当前假设类 未检出"，joint p=0.0167 → df 校正后 0.1131 不显著。
2. **前瞻段**（> n_basis）：本协议 df=1 序贯。
3. 两段**禁止合并、禁止 Fisher、禁止互相校正 p 值**。前瞻确认 ≠ 历史发现的独立复现
   （方向向量来自历史段，存在选择效应；确认的意义 = "历史信号延续到未来"，仅此）。
4. **不得追加统计量、不得改边界、不得改 n_max**——任何修改 = REGISTRY_TAMPERED，
   锚点校验失败即拒绝打分。重开协议须新文件 v2 + 新锚点 + 明确理由。

## 5. 与其他仪器的关系

- `predict_tonight.py`（预测登记/打分）：不受影响，照常运行。
- 旧 Bonferroni 窥视打分：降级为 `--legacy`（仅历史复算用）。
- 贝叶斯物理先验（次级证据，`audit/PHYSICAL_PRIOR_BAYES.md` 待建）：
  制造公差（直径 0.12%、质量 3.75%）vs 所需 σ_θ>3.5% 的量纲冲突可定量化为
  P(制造解释|公差)≈O(1e-3)。**注意**：它同时压低候选先验，不是确认证据。
  映射常数 c∈[0.5,2] 暂无文献来源 ⇒ **TBD**（不许写具体数）。
- 物理测量（`audit/PHYSICAL_MEASUREMENT_PROTOCOL.md`）：仍唯一"周/月"级路径，随时可启动。

## 6. 诚实时间表（σ=3.5% 为真时）

| 里程碑 | n | 年 | 概率 |
|---|---|---|---|
| 中位确认 | ~1189 | ~7.9 | 50% |
| 功效 84% | 1500 | 10.0 | — |
| 功效 97% | 2000 | 13.3 | — |
| 终点判决 | 3500 | 23.3 | 99.75% |

若 σ 真值更高（≥5%），中位确认提前至 ~500 期（3.3 年）；若更低，终点前未必穿越。

---
锚定：本文件 sha256 + 边界表 sha256 写入 `anchors/preregistered.sha256`（git 提交即时间戳）。
