# 三引擎联合 null 坐实报告（诚实闸门版）

生成时间：2026-08-24
作者：AI（自主推进，紧鞘闸门不放松）

## 一、背景与动机
- evolve_predictor（线性加权 top6 + 跨期特征）探明 z≈0.45 → null。
- 假设1"表达力不足"：升级 seq_evolve（序列岭回归，线性加权真超集）→ 仍 null（确认 z 最高 0.325）。
- 假设2"蓝球维度信号密度更高"：blue_evolve（蓝球 16 选 1 单独建模）→ 仍 null（确认 z 最高 0.382）。
- 假设3"非线性序列依赖"：gru_evolve（纯 numpy GRU，捕捉长期非线性转移）→ 仍 null（确认 z 最高 0.630）。

## 二、三引擎网格结果汇总
| 引擎 | 配置数 | 确认段 z 最高 | surrogate z 最高 | 结论 |
|------|--------|--------------|------------------|------|
| evolve_predictor（线性加权） | 多组 | 0.451 | 0.544 | null |
| seq_evolve（序列岭回归） | 18 | 0.325 | 2.226(K=8) | null（surrogate>2=学噪声） |
| blue_evolve（蓝球单独） | 24 | 0.382 | 1.153 | null |
| gru_evolve（numpy GRU） | 27 | 0.630 | 1.650 | null |

**四引擎（含最初 evolve_predictor）确认段 z 全部 < 2，无任何突破。**

## 三、关键反证（紧鞘闸门拦截构造伪显著）
- seq_evolve 在 K=8 时 surrogate_z=2.226（>2）：随机打乱标签后同架构仍能拟合出同量级伪信号 → 证明该架构在学噪声非结构。闸门正确拦截。
- gru_evolve surrogate 最高 1.65（<2），blue_evolve 最高 1.15（<2）：未触发伪显著，但确认段同样 <2 → 无真信号。

## 四、结论（诚实，不美化）
双色球开奖数据在当前所有被探索的模型空间（线性加权 / 序列线性映射 / 蓝球单独 / 非线性 GRU 序列）中，
**一致指向 null 域（= 无可被这些架构稳定捕捉的 >随机 结构）。这是一个"经多引擎、多架构、紧鞘闸门交叉验证"的强证据，但仍是待检验猜想而非定论**——
未来若出现全新信号源（物理开奖机参数、跨彩种耦合、非平稳结构突变检测等），结论可被推翻。

## 五、算力投入与产出（透明）
- 本地 8 核并行 + GitHub Actions 矩阵（每 6h × 8 worker）已激活，每日评估量级万级。
- 本次新增 3 个独立引擎脚本（seq/blue/gru），共扫描 69 组超参配置，全部诚实走紧鞘。
- 算力是必要不充分：更大算力加速探索更大模型空间，但不能凭空造信号。本次把"表达力""蓝球维度""非线性序列"三个假设逐一证伪，正是算力的价值——快速、诚实、可复现地排除错误方向。

## 六、下一步建议（待用户拍板，红线不变）
- 选项 A：引入物理/外部信号源（开奖机序列号、摇奖时长、温湿度等）——需数据采集。
- 选项 B：非平稳结构突变检测（用变点分析找"历史某段是否存在短暂可预测窗口"）。
- 选项 C：正式接受"四引擎一致 null"，归档为研发结论，转下一课题（符合用户"想通即放弃不恋战"风格）。
- **任何声称 >随机 的结论，必须经过紧鞘（surrogate + 独立确认段复现 z>2 + 进化不可见确认段），绝不放松凑 >2。**

## 七、环境坑记录
- torch CPU wheel(~120MB) 经本机代理下载两次均 IncompleteRead 断流；改用纯 numpy 手写 GRU（数学等价，对 33 维小输出足够），不阻塞。若未来需 Transformer 大规模训练，建议离线下载 wheel 或换非代理源。
- **numpy 多线程线性代数在沙箱原生崩溃**（exit1 无 traceback，非 Python 异常）：`changepoint_evolve.py` 大量重复 330×330 `np.linalg.solve` 时触发。已通过模块顶部 `os.environ` 设 `OMP/OPENBLAS/MKL_NUM_THREADS=1` 根治。所有 numpy 密集脚本若遇偶发静默崩溃，优先排查此坑。

## 八、变点/非平稳结构检测（选项 B，已完成）
- 新增 `changepoint_evolve.py`：滑动窗口扫描全历史，探测"是否存在某段局部可预测窗口"（全历史平均会稀释非平稳结构）。
- **多重比较校正（Bonferroni，α=0.05 双侧）**：扫描 n 个窗口，临界 z = Φ⁻¹(1-0.05/2n)。单窗口 z>2 不算数，必须过校正阈值才声称局部结构（防假阳性）。
- 5 种窗口配置（W∈[200,250,300,400,500], step∈[50,100,150,200], fit_back∈[400,500,600]）全部稳定扫描：
  | W | step | fit_back | n_win | crit | passed | max_raw_z |
  |---|------|----------|-------|------|--------|-----------|
  | 300 | 150 | 400 | 19 | 3.008 | 0 | 1.778 |
  | 200 | 100 | 400 | 29 | 3.134 | 0 | 2.593 |
  | 500 | 200 | 600 | 12 | 2.865 | 0 | 1.420 |
  | 400 | 100 | 500 | 26 | 3.102 | 0 | 2.053 |
  | 250 | 50 | 400 | 57 | 3.327 | 0 | 2.644 |
- **结论：所有配置 passed=0，最大原始 z 最高仅 2.644（未校正），全部低于校正临界（≥2.865）。连局部都无稳定可预测窗口 → "非平稳变点"假设也被排除。**
- 注：初版未校正时曾出现单窗口 z=2.044（贴线），加 Bonferroni 后正确剔除——印证多重比较校正的必要性，也印证紧鞘闸门不放水的原则。

## 九、五引擎联合最终结论（诚实，不美化）
| 引擎 | 方法 | 确认/局部 z 最高 | 结论 |
|------|------|------------------|------|
| evolve_predictor | 线性加权 top6 | 0.451 | null |
| seq_evolve | 序列岭回归 | 0.325 | null |
| blue_evolve | 蓝球单独 | 0.382 | null |
| gru_evolve | numpy GRU | 0.630 | null |
| changepoint_evolve | 变点检测(校正) | 2.644(未校正)/0通过 | null |

**五引擎、多架构、含多重比较校正与随机 surrogate 对照，一致指向 null 域（= 双色球开奖历史数据无可被这些架构稳定捕捉的 >随机 结构）。这是经严格方法学交叉验证的强证据，但仍是待检验猜想而非定论**——未来若出现全新信号源（开奖机物理参数、跨彩种耦合、外部时序耦合）可被推翻。

## 十、研发建议（待用户决策，红线不变）
- 选项 A：引入物理/外部信号源（开奖机序列号、摇奖时长、温湿度、公证录像帧特征）——需数据采集链路。
- 选项 C：正式接受"五引擎一致 null"归档为研发结论，转下一课题（符合用户"想通即放弃不恋战"风格）。
- 任何声称 >随机 的结论，必须经过紧鞘（surrogate + 独立确认段/多重比较校正复现 z>2 + 进化不可见确认段），绝不放松凑 >2。
